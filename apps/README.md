# Apps

Entry points for deployable applications. This area will host the web client and server runtime built from the shared packages in the monorepo.

- `web`: browser-facing client application.
- `server`: backend services and API surface.
